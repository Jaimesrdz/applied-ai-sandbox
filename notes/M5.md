# M5 — Task Decomposition

## Decomposition (8-10 tasks)
1. **Task 1 — <name>**: Add to the existing dictionaries inside of the app to include tags. _Verify: Ensure existing tests pass with the new addition._
2. **Task 2 — <name>**: Create a list of options that the user will choose from . _Verify: Create a new test that ensure only those options can be chosen._
3. **Task 3 — <name>**: Provide a dropdown menu with tag options.  _Verify: Launch the app and test it by creating a new note._
4. **Task 4 — <name>**: Update the new note's tags to what the user chose by using a parser to save it to the dictionary. _Verify: Open the app and enter tags using dropdown menues._
5. **Task 5 — <name>**: Create a filter option. _Verify: Choosing a specific filter will only show results containing the chosen filter._
6. **Task 6 — <name>**: Ensure that the filter saves when the user comes back to the home page. _Verify: Change the filter and navigate pages on the site, then return to home and ensure the filter stays as it was left._

## Task 1 context brief
The end goal is to have "tags" available for use inside of the app, and that tests return green with the addition.
Change app.py to include a key names "tags" within the dictionaries. This key will be used as a filtering option for the user that they can add to their notes. 
Ensure that existing tests pass without changing them, do not edit tests.
Plan out your steps and reasoning. Do not write any code yet.

## Task 1 — what happened
- Claude's plan summary: 
    Plan: Add tags key to note dictionaries (Task 1)

     Context

     This is Task 1 of M5 in the Applied AI Cohort sandbox. The goal is to lay the
     groundwork for a tag-based filtering system. Right now each note is stored as
     {"title": ..., "body": ...}. Future tasks (dropdown UI, parser, filter view)
     all depend on each note having a "tags" key. Task 1 just adds that key so
     the data model is ready — no UI or filtering logic yet.

     Existing tests must continue to pass without modification.

        What the tests actually check

     
     
     │                   Test                   │                 What it asserts about notes
    
     │ test_valid_submit_redirects_and_persists │ any(n["title"] == "First" for n in app.notes) — only checks title
    
     │ test_delete_removes_note                 │ Seeds notes manually, checks app.notes[0]["title"] — only checks title

     │ All other tests                          │ HTTP status codes and HTML text — don't inspect dict keys

    No test inspects a "body" or "tags" key specifically, so adding "tags"
     cannot break them. Tests that manually seed notes (like in task 02) append
     {"title": ..., "body": ...} directly — that's fine because nothing in the
     app currently reads tags back.

     Change required

     File: app.py — line 29

     Current:
     app.notes.append({"title": title, "body": body})

     After:
     app.notes.append({"title": title, "body": body, "tags": []})

     tags defaults to an empty list because a note can have zero or more tags,
     and a list is the natural container for future multi-select values.

     That is the only change needed for Task 1.

     Verification

     1. Run pytest — all existing tests should still be green.
     2. Manually confirm the new key exists: python app.py, POST a new note,
     then check app.notes in the running process (or add a quick print
     temporarily) to see {"title": ..., "body": ..., "tags": []}.

- What I pushed back on: Nothing, everything looked great.
- Verification result: Ran pytests myself and concluded that all tests that had to deal with the notes still passed. All failers were pre-existing before any changes.

## Task 2 — what happened
- ...

## Task 3 — what happened
- ...

## Reflection — where I redirected
- (3 sentences)