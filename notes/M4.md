# M4 — Reading and Verifying Output

## Bug hunt #1 — SQL injection
- Bug: The program has no way to understand the difference between a search term and a query, creating a risk for malicious attacks.
- Why Claude wrote it: Likely because of the common structure that comes with the use of the f-string, since the LLM uses common patterns, it would require additional context to avoid doing this.
- Fix: Send the query and input data separately through the use of parameters:
    ```python
    "SELECT id, title FROM notes WHERE title LIKE :q",
    {"q": f"%{q}%"}
    ```

## Bug hunt #2 — hallucinated package
- Bug: The flask_simpleauth package does not exist.
- Why Claude wrote it: Probably found a source indicating this package was real when in reality it wasn't.
- Fix: Replace the import with the real package Claude is attempting to utilize.

## Bug hunt #3 — edge case
- Bug: A user could input an empty list, leading to a ZeroDivisionError.
- Why Claude wrote it: Claude wrote the test in a way that would return as green instead of testing edge cases that could lead to errors
- Fix: Only run the code if the list is actually populated with floats.

## Bug hunt #4 — hardcoded secret
- Bug: API key is in the main file, which would cause the key to stop working if it were pushed onto a repo.
- Why Claude wrote it: There was likely no existing .gitignore file, so Claude just put the code into the main file.
- Fix:Create a .gitignore file or .env file and move the api key into it.

## Test I wrote
- Bug I picked: Edge Case
- Test code:
```python
def average_rating(ratings):
    try:
        if not ratings:
            return None
        return sum(ratings) / len(ratings)
    except TypeError:
        return None
```
- Confirmed it fails on the bug and passes on the fix? Yes

## Near-miss reflection
- I can't think of anything, so I'll be answering the prompt under the help option.
I usually try to utilize Claude when working with tasks that I don't understand, and because of that, it's easy to just trust whatever Claude is doing and think it's correct. However, since I want to learn something from the experience, I usually ask Claude to explain the code before I utilize it, even if I don't completely understand it.